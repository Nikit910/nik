/**
*
*
#include<stdio.h>

int main(void){
int a, b,c;
cmd = '\0';
}
while(1){
  scanf("%lg%c%lg", &a, &cmd, &b);
  
  switch(cnd){
  case'+':
      printf("%lg + %lg = %lg\n", a, b, a + b);
      break;
  case'-';
      printf("lg - %lg = %lg\n", a, b, a - b);
      break;
  case'*':
      printf("lg * %lg = %lg\n", a, b, a * b);
      break;
  
